node_modules
dist/
.next
.next.prev
out
tmp/
*.tsbuildinfo
.env*.local
.env
*.db
*.db-shm
*.db-wal
.worktrees/

# Cloudflare Tunnel credentials (per-host, never share)
cloudflared.local.yml
*.cloudflared.json
.env.test
.env.docker
.pi/
.worker-sessions/
.worker-sockets/

# Worker outbox spool when SESSION_ROOT is unset (root falls back to cwd)
/channel/
