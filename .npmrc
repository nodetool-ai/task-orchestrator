# The Claude Agent SDK declares a zod@^4 peer, but ships against the zod 3.25+
# `zod/v4` subpath that this project already provides — so the mismatch is
# cosmetic. Allow installs to proceed instead of failing on the peer conflict.
legacy-peer-deps=true
